[phases.setup]
nixPkgs = ["python311Full", "playwright"]

[phases.install]
cmds = ["pip install -r requirements.txt", "playwright install --with-deps chromium"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 0 app:app"
